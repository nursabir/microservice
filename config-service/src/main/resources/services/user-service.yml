spring:
  jpa:
    database: POSTGRESQL
    properties.hibernate.temp.use_jdbc_metadata_defaults: false
    hibernate:
      ddl-auto: create
    properties:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/user_service_database
    username: postgres
    password: 148868
    driver-class-name: org.postgresql.Driver

server:
  servlet:
    context-path: /users
    port: 8081